# Figma OAuth Credentials for Local Development
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file - it's gitignored for security

# Figma OAuth Application Credentials
# Get these from: https://www.figma.com/developers/apps
FIGMA_CLIENT_ID=your_figma_client_id_here
FIGMA_CLIENT_SECRET=your_figma_client_secret_here

# Coda Pack Configuration
CODA_PACK_ID=44800

# Notes:
# - These credentials are for local development and testing only
# - Production OAuth is configured directly in Coda Pack settings
# - Never commit actual credentials to version control
# - Rotate credentials if they become exposed